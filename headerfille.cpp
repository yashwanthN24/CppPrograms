#include<iostream> // this is for including the system header files 
#include"this.h"  // this is for including the user defined header files 

// system header files are the header files that comes by default with the compiler 
// we can define our own header files as well and include the as #include "userdefinedheaderfile.h"
using namespace std;
int main()
{
      cout << "Operators in c++ " << endl ; 
} 
